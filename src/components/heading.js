import styled from 'react-emotion';

const Heading = styled('h2')({
  display: 'inline-block',
  margin: '.5rem 0',
  padding: 0,
  color: '#1e1e1e',
  fontSize: '1.5rem',
  fontWeight: 600,
  lineHeight: '2rem',
});

export default Heading;
