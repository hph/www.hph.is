import styled from 'react-emotion';

const Title = styled('h1')({
  margin: 0,
  padding: 0,
  color: '#1e1e1e',
  fontSize: '2rem',
  fontWeight: 600,
  lineHeight: `${36 / 16}rem`,
});

export default Title;
