import styled from 'react-emotion';

const Note = styled('aside')({
  margin: 0,
  padding: 0,
  marginBottom: '1.2rem',
  paddingLeft: '1rem',
  fontSize: '0.9rem',
  fontWeight: 400,
  fontStyle: 'italic',
  color: '#4e4e4e',
  lineHeight: '1.65rem',
  borderLeft: '2px solid #0085ff',
});

export default Note;
